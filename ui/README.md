# UI

React and Vite frontend for the desktop crawler.

## Commands

```bash
npm install
npm run build
npm run dev
```

The desktop shell loads the built files from `ui/dist`, which is generated locally and ignored by Git.
